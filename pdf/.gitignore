# Runtime dependencies (installed via `npm install` in the Docker image build).
node_modules/
# Generated bundle of the shared chart builders (built via `npm run bundle-charts`).
vendor/
# Build-time staged copies (populated by build.sh from canonical sources).
src/
assets/
# Python bytecode.
__pycache__/
*.pyc
